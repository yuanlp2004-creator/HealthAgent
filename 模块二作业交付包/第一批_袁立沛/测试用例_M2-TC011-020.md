# 模块二新增血压测试用例

袁立沛负责范围（M202677260），计划50%。用例与执行由AI辅助完成，待本人复核，不记为本人独立完成的证明。以下边界来自现有API字段约束，属于软件输入测试，不是临床诊断阈值。每行对应一个pytest测试项，行内多次请求不单独计数。

|编号|标题|前置条件|输入|步骤|预期结果|优先级|方法|
|---|---|---|---|---|---|---|---|
|M2-TC-011|数值下边界与外侧拒绝|注册用户，SQLite内存库|收缩压60、舒张压30、心率30；分别改为59、29、29|先创建下边界记录，再单独降低各字段；查询总数|合法值201并原样返回；三次外侧输入422；总数仍1|中|边界值、数据不变性|
|M2-TC-012|数值上边界与外侧拒绝|注册用户，SQLite内存库|收缩压260、舒张压200、心率220；分别改为261、201、221|先创建上边界记录，再单独提高各字段；查询总数|合法值201并原样返回；三次外侧输入422；总数仍1|中|边界值、数据不变性|
|M2-TC-013|非法心率更新保留原值|创建心率72的记录|PATCH heart_rate=29，再传221|逐次提交非法更新，每次GET原记录|两次422；记录可读且心率仍72|中|无效等价类、状态保持|
|M2-TC-014|备注长度边界及更新原子性|注册用户|创建500个N；更新500个B，再更新501个B|POST后PATCH两次，最后GET|创建201；500字符更新200；501字符422；最终仍为500个B|中|边界值、场景法|
|M2-TC-015|图片引用长度边界|注册用户；不调用OCR服务|source=ocr，image_id为64个I和65个I|分别POST并查询列表总数|64字符201且字段保留；65字符422；总数1|中|边界值；只测引用字段|
|M2-TC-016|越权修改删除不影响所有者记录|分别注册所有者和另一用户|所有者创建120/80、note=owner only；另一用户PATCH systolic=200并DELETE|另一用户执行两次写操作；所有者GET并查总数|越权PATCH和DELETE均404；所有者记录仍120、备注未改；总数1|高|权限隔离、状态保持|
|M2-TC-017|统计隔离与空心率平均值|固定服务时钟为2026-09-14T12:00:00Z；两个用户|本人三条120/80/null、121/82/71、124/85/74；他人260/200/220|分别建记录，查询本人stats days=7|200；count=3；收缩压均值121.7、范围120至124；舒张压82.3、范围80至85；心率72.5|高|计算核对、用户隔离、空值|
|M2-TC-018|统计窗口下界微秒精度|固定服务时钟；心率均为空|7天下界前1微秒收缩压200，下界120，后1微秒130|创建三条，查询stats days=7|200；仅计2条；收缩压均值125、范围120至130；心率均值null|中|时间边界、固定时钟|
|M2-TC-019|闭区间过滤与分页排序|注册用户|start前1微秒、start、end、end后1微秒各一条；size=1|查询同一区间第1、2、3页|均200且total=2；第一页end记录，第二页start记录，第三页空；分页字段正确|中|边界值、排序分页组合|
|M2-TC-020|查询参数上下界|注册用户，空记录库|page=1/0；size=1/200/0/201；stats days=1/365/0/366；forecast days=1/30/0/31|逐次GET列表、统计或预测接口|所有有效边界200；所有外侧值422|中|接口参数边界值|

## 实测与去重

源码：backend/tests/module2/test_bp_ai.py。首次个人执行目录：执行结果/20260914_211410_600392，10通过、0失败、0跳过。完整20条执行目录：执行结果/20260914_212935_082440，20通过。每组environment.json包含99318c9基线、未提交工作区状态及源码哈希。

模块一仅测一般CRUD、超大收缩压、读取隔离和普通分页；本批增加逐字段端点、写权限及失败后原值、空心率平均数、精确窗口边界和过滤分页组合。模块一D02的必填null、D03的时区等价性不在本批重新编号。017和018仅冻结业务统计时钟，不冻结JWT时钟。

POST统一使用/api/v1/bp-records；单条PATCH和GET附记录id。实际输出见对应日志及断言，Excel填OK仅代表这些断言通过，不证明覆盖全部功能。020只检查预测参数，不验证预测算法精度。
